# План улучшений: Dynamic Semantic Window

Дата: 2026-07-04. Основан на аудите ядра стратегии, стенда и HPO-контура.

Логика плана: сначала валидность (этап 0), затем честная измерительная база
(этап 1), ускорение стенда (этап 2) — и только потом научные гипотезы (этап 3),
потому что до этапов 0–1 результаты экспериментов неинтерпретируемы.

Трудоёмкость: S — до полудня, M — 1–2 дня, L — 3+ дней.

---

## Этап 0. Валидность алгоритма и данных

**Цель этапа:** боевой путь и HPO-суррогат считают одно и то же; данные
корректно сегментированы.

### Шаг 0.1. Фикс бага левой экспансии — S — ✅ выполнено (2026-07-04)

- В `src/dynamic_retriever.py` (левая ветка `_expand_cluster`) `prev_id`
  берётся от seed-узла на каждой итерации, а не от текущего узла. Из-за этого
  левая экспансия фактически ограничена одним предложением независимо от
  параметров, а кластер до дедупликации наполняется дубликатами.
- Регрессионный тест: на синтетическом документе с однородной левой
  окрестностью левая экспансия достигает `max_expand`, кластер не содержит
  дубликатов.
- **Готово, когда:** тест зелёный; прогон `stand run` показывает изменение
  токенов/HR (зафиксировать — это «настоящий» алгоритм, старые результаты
  невалидны).

### Шаг 0.2. Единое ядро алгоритма — L (ключевой шаг этапа) — ✅ выполнено (2026-07-04)

> Итог: ядро — `src/expansion_core.py` (`DynamicExpansionCore`), адаптер —
> `src/dynamic_retriever.py`, `src/cached_expander.py` удалён. Parity-тест:
> 20 рандомизированных корпусов, адаптер ≡ ядро. Решённые расхождения
> задокументированы в докстринге ядра. Seed validation удалён из пайплайна
> (был отключён и признан ошибочным; конфиг помечен DEFERRED).
>
> **Критичная находка:** `VectorStoreIndex` перезаписывает узлы в docstore
> копиями без эмбеддингов, поэтому старый боевой путь всегда читал
> `embedding=None` — проверки схожести, query-aware, skip и backfill молча
> не работали, и «Dynamic Semantic» в старых бенчмарках был фактически
> фикс-окном ±1 с merge. Все старые результаты (README, results/) невалидны
> вдвойне; пороги по умолчанию (threshold=0.96 и др.) никогда не работали в
> боевом пути и требуют ретюна после 0.3/0.6. Новый адаптер получает массивы
> напрямую от стратегии и от docstore не зависит.

- Обобщить `CachedDynamicExpander` до единственной реализации экспансии:
  чистая функция над массивами (`neighbor_sims`, `sentence_sims`, индексы)
  без I/O и без LlamaIndex.
- `DynamicSemanticExpander` превратить в тонкий адаптер: собрать массивы из
  docstore → вызвать ядро → собрать `TextNode`.
- Перенести в ядро всё, что сейчас есть только в боевом пути и включено
  (adaptive threshold, gradient stop) — либо осознанно удалить, если по итогам
  3.2 их заменит depth-детекция.
- Parity-тест: на случайных корпусах оба адаптера дают идентичные кластеры
  (property-based).
- **Готово, когда:** parity-тест зелёный; в `dynamic_retriever.py` не осталось
  собственной логики экспансии.

### Шаг 0.3. Согласовать суррогат с боевым путём по данным — M — ✅ выполнено (2026-07-04)

> Итог: HPO-корпус строится тем же режимом эмбеддинга, что и боевой
> `dynamic_semantic`: тексты для эмбеддинга собирает общий helper
> `build_embedding_texts`, а ключ corpus-cache включает `sentence` или
> `phantom_wN`. Для tuning добавлен явный `--phantom-window`, по умолчанию
> `1`, чтобы совпадать с `DynamicSemanticConfig`. Критерий «ответ найден»
> вынесен в `src/answer_matching.py` и используется и в `src/metrics.py`, и в
> `src/corpus_data.py`: сначала нормализованный substring, затем fuzzy fallback.
>
> Решение: `run_optuna.evaluate_params` стал общей функцией оценки HPO-параметров
> для objective, train и val. Это оставляет единственный вычислительный контракт
> поверх `DynamicExpansionCore`; отдельная HPO-метрика больше не дублируется.

- Корпус для HPO (`stand/tune.py`) строить на тех же эмбеддингах, что использует
  бенчмарк: если `phantom_window=1` — фантомных. Режим эмбеддинга включить в
  ключ кэша корпуса.
- Унифицировать критерий «ответ найден»: один модуль сопоставления
  (нормализованный substring + fallback fuzzy), используемый и в
  `src/metrics.py`, и в `src/corpus_data.py`.
- **Готово, когда:** метрики HPO-прогона на тех же параметрах воспроизводят
  метрики `stand run` с точностью до долей процента (сквозной smoke-тест).

Контекст: у соседних предложений фантомные тексты пересекаются на 2 из 3
предложений, поэтому adjacency-косинусы в фантомном пространстве систематически
завышены. Пороги, найденные Optuna в «чистом» пространстве, некорректно
переносятся в фантомное.

### Шаг 0.4. Изоляция документов в shared-режиме — M — ✅ выполнено (2026-07-04)

> Итог: `DynamicSemanticStrategy._build_index` больше не склеивает документы в
> один текст. Предложения, phantom-контекст и `prev_id`/`next_id` строятся
> пер-документно по `source_doc`; `stand.runner` передаёт `source_doc` и title в
> metadata. В `DynamicExpansionCore` добавлен `segment_ids`: обязательное
> `min_window`, skip и merge не переходят через границы сегмента/документа.
>
> Решение: shared-индекс остаётся общим только для первого retrieval-pass
> (конкуренция документов сохраняется), но расширение seed'а и слияние
> кластеров всегда ограничены документом seed'а.

- В `src/strategies.py` (`DynamicSemanticStrategy._build_index`) все документы
  склеиваются в один текст и одну цепочку предложений: экспансия и фантомный
  контекст перетекают через границы документов. Строить цепочки пер-документно
  (`doc_id` из `source_doc`), запрет экспансии через границу документа.
- Тест: в shared-индексе из двух документов экспансия seed'а у границы не
  захватывает чужие предложения.

### Шаг 0.5. Сегментация предложений — S/M — ✅ выполнено (2026-07-04)

> Итог: общий `split_into_sentences` в `src/utils.py` использует `pysbd`
> (`requirements.txt`) и имеет fallback без внешней зависимости для smoke-тестов
> и окружений, где пакет ещё не установлен. Тот же сегментатор используется при
> индексации, построении HPO-корпуса и поиске `answer_sentence_idx`.
>
> Решение: QASPER before/after долю найденных `answer_sentence_idx` надо
> фиксировать при следующем полном прогоне на QASPER-артефакте; локальный
> регрессионный тест закрывает критичные сокращения `e.g.`, `Fig. 3` и
> десятичные числа.

- Заменить regex `(?<=[.!?])\s+` в `src/utils.py` на pysbd (или syntok);
  сегментатор — общий для индексации, корпуса и поиска ответа. Regex ломается
  на «e.g.», «Fig. 3», инициалах, десятичных числах — для QASPER это массово.
- Метрика приёмки: доля вопросов с найденным `answer_sentence_idx` на
  QASPER-корпусе выросла (зафиксировать до/после).

### Шаг 0.6. Устранить утечку тюнинга — M — ✅ выполнено (2026-07-04)

> Итог: `stand tune` делает document-level split через
> `split_corpus_by_documents`: по умолчанию 70% документов train, 30% val,
> `--split-seed 42`. Optuna оптимизируется только на train-корпусе. После
> выбора best trial параметры оцениваются общей `evaluate_params` на train и
> val, а tuned-манифест содержит `metrics_train`, `metrics_val` и `tuning`
> metadata. `stand run --params tuned` включает val-ожидание из манифеста в
> result JSON и печатает его рядом с текущим прогоном.
>
> Решение: при корпусе из одного документа split деградирует в train=val для
> smoke-тестов; для реальных отчётов такой tuned-артефакт не считается честной
> held-out оценкой.

- `stand tune` оптимизирует на всех вопросах датасета, `stand run
  --params tuned` оценивает на нём же — train-on-test.
- Document-level split (не question-level — вопросы одной статьи
  коррелированы): тюнинг на 70% документов, отчёт по held-out 30% (позже
  k-fold). Обе метрики в манифест tuned-артефакта.
- **Готово, когда:** манифест содержит `metrics_train` и `metrics_val`, и
  `stand run --params tuned` сравнивается с val-ожиданием.

### Ревью-фиксы после 0.3–0.6 (2026-07-04)

> По итогам ревью реализации 0.3–0.6 исправлено:
>
> 1. **Chunk-level критерий ответа.** Fuzzy set-coverage (порог 0.70) в
>    `contains_answer` давал ложные хиты на несвязанных чанках за счёт
>    служебных слов (проверено: score 0.70–0.89 на чанке о другой теме) и
>    инфлировал HR/MRR/NDCG, сильнее для крупночанковых стратегий. Заменён
>    на точное нормализованное вхождение (с учётом границ токенов) +
>    fallback по непрерывному окну токенов ответа (покрытие ≥ 0.90) — только
>    для толерантности к обрезке предложения границей чанка. Fuzzy-матчинг
>    сохранён на sentence-уровне (`find_answer_sentence_idx`), где кандидаты
>    короткие. Известное следствие: для перефразированных answer-текстов
>    HPO (span-based hit) может расходиться с бенчмарком (text-based hit) —
>    закрывается резолвингом ответа в предложение на стороне раннера (не в
>    рамках этапа 0). Контрактные тесты: `tests/test_answer_matching.py`.
> 2. **Метаданные в эмбеддингах бейзлайнов.** `_document_from_item` теперь
>    выставляет `excluded_embed_metadata_keys`/`excluded_llm_metadata_keys`:
>    naive/fixed/token/semantic больше не эмбеддят `source_doc:/title:` в
>    каждом чанке (иначе — молчаливое изменение всех бейзлайнов).
> 3. **Вырожденный split.** `split_corpus_by_documents` при <2 документах
>    предупреждает о полной утечке (val == train).

### Гейт этапа 0

Перепрогнать полный бенчмарк (per_document + shared) на исправленном алгоритме
и данных → это новая базовая линия. Старые числа в README пометить как
устаревшие.

> Решение gate: старые таблицы README помечаются как legacy/outdated, потому что
> они были получены до исправлений 0.2–0.6. Новая базовая линия должна
> публиковаться только из свежих `stand run` результатов после этих изменений,
> отдельно для `per_document` и `shared`.
>
> Gate-smoke (2026-07-04): свежий полный matrix-прогон всех стратегий на
> локальном `wiki_10` + `bge-small` завершён для обоих режимов:
> `results/benchmark_wiki_10_per_document_20260704_153140.json` и
> `results/benchmark_wiki_10_shared_20260704_154137.json`. Попытка полного
> `wiki_100_qa` + `bge-small` gate была запущена, но не уложилась в 15 минут
> на текущем небатченном стенде; публиковать новую README baseline нужно после
> отдельного полного прогона `wiki_100_qa` (желательно после ускорений 2.1–2.2
> или с увеличенным лимитом времени).
>
> **Гейт закрыт (2026-07-04, после 2.1–2.2 и 1.1):** полный `wiki_100_qa` +
> `bge-small`, все 5 стратегий, default params, tiktoken-счёт токенов:
> `results/benchmark_wiki_100_qa_per_document_20260704_210713.json` (76 с,
> тёплый кэш: 51824 hits / 0 misses) и
> `results/benchmark_wiki_100_qa_shared_20260704_211717.json` (539 с).
> Это новая базовая линия. Кросс-проверка счёта токенов: shared-прогон с
> chars/4 (20:13) и с tiktoken (21:17) дают у бейзлайнов идентичные
> HR/MRR/P/NDCG, токены ниже на ожидаемые ~10%. Содержательно: Dynamic
> Semantic на default-параметрах лидирует по HR/MRR/NDCG (0.990/0.942/0.954
> per_document; 0.977/0.896/0.917 shared), но тратит на ~55–60% больше
> токенов, чем Fixed Window (1694 vs 1064; 1594 vs 1021) — тезис «то же
> качество за меньше токенов» на дефолтах не показан, ответ дадут ретюн
> (пороги default никогда не работали в старом боевом пути, см. 0.2) и
> кривые HR-vs-токены (1.3).

---

## Приоритетный блок П. Потолок бенчмарка и dual-space эмбеддинги (2026-07-04)

**Мотивация.** Свежая базовая линия гейта косвенно говорит, что на
`wiki_100_qa` ответное предложение почти всегда уже в зёрнах первого прохода
(HR@5 dynamic = 0.990 при потолке ~1.0; P@5 ≈ 1/5 — ровно один релевантный
кластер; выигрыш экспансии виден только в MRR/NDCG, т.е. в ранжировании).
Если это так, польза экспансии на текущем датасете неизмерима в принципе, и
любые эксперименты этапа 3 будут меряться об потолок. Параллельно найден
конструктивный дефект сигнала экспансии: adjacency-косинусы считаются в
фантомном пространстве, где тексты соседей при w=1 пересекаются на 2 из 3
предложений — сигнал «граница темы» систематически завышен и сжат.

Блок делается до этапа 1 (кроме уже выполненного 1.1) и до остальных
экспериментов этапа 3. Порядок внутри блока строгий: П.1 → П.2 → П.3.

### Шаг П.1. Ablation seeds-only: проверка гипотезы потолка — S — ✅ выполнено (2026-07-04)

- Прогон dynamic_semantic с выключенной экспансией (`min_window=0`,
  `max_expand=0`, `merge_gap=0` — ядро вырождается в одиночные
  предложения-зёрна) на `wiki_100_qa` + `bge-small`, оба режима индекса,
  тёплый кэш. Сравнить HR/MRR/tokens с базовой линией гейта.
- **Интерпретация:** если seeds-only HR ≈ базовой (в пределах ~1 п.п.),
  гипотеза потолка подтверждена — на этом датасете экспансия не добавляет
  хиты, и её ценность можно измерять только на П.2-датасете (и/или qasper).
  Разница MRR покажет, какая часть выигрыша dynamic — чистое ранжирование
  (слияние кластеров), а не поиск.
- **Готово, когда:** результат-JSON с seeds-only конфигурацией сохранён и
  выводы зафиксированы в этом документе.

> Итог: в `stand run` добавлен `--dynamic-overrides` (JSON поверх
> default/tuned, пишется в result JSON). Прогнаны два ablation'а
> (default-параметры остальных полей, тёплый кэш):
>
> | конфиг | режим | tokens | HR@5 | MRR |
> |---|---|---:|---:|---:|
> | seeds-only (0/0/0) | per_document | 145 | 0.870 | 0.579 |
> | seeds-only (0/0/0) | shared | 145 | 0.840 | 0.565 |
> | окно ±1 (1/1/0) | per_document | 564 | 0.977 | 0.872 |
> | окно ±1 (1/1/0) | shared | 535 | 0.960 | 0.856 |
> | default dynamic (база гейта) | per_document | 1694 | 0.990 | 0.942 |
> | default dynamic (база гейта) | shared | 1594 | 0.977 | 0.896 |
>
> Файлы: `..._per_document_20260704_213546.json` (seeds-only),
> `..._shared_20260704_214126.json` (seeds-only),
> `..._per_document_20260704_214319.json` (±1),
> `..._shared_20260704_214841.json` (±1).
>
> **Вывод 1 — гипотеза потолка в строгой форме опровергнута:** seeds-only
> HR = 0.87/0.84, на 12–14 п.п. ниже базы. Ответ не лежит в самом зерне;
> фантомный эмбеддинг (w=1) «размазывает» ответ на соседей, seed часто
> садится рядом с ответным предложением, и первый шаг экспансии его
> добирает. Экспансия делает реальную работу.
>
> **Вывод 2 — почти вся ценность в первом шаге:** окно ±1 возвращает
> 0.977/0.960 HR за 564/535 токенов. Дальнейшая экспансия default-параметров
> (+1130 токенов, ×3) даёт лишь +1.3/+1.7 п.п. HR (внутри CI ±0.025) и
> +7/+4 п.п. MRR. Глубокая экспансия на этом датасете почти не окупается —
> но это и есть зона, где различия сжаты потолком, поэтому П.2 остаётся
> обязательным для оценки глубокой экспансии и П.3.
>
> **Вывод 3 — тезис проекта уже демонстрируется в точке ±1:** dynamic с
> окном ±1 держит HR/MRR не хуже fixed_window (0.977/0.872 vs 0.970/0.853
> per_document; 0.960/0.856 vs 0.957/0.838 shared) при ~53% его токенов
> (564 vs 1064; 535 vs 1021). Это сильный кандидат в «экономичную» точку
> Парето для кривых 1.3.

### Шаг П.2. Перефраз вопросов → `wiki_100_qa_hard` — M — ✅ выполнено (2026-07-04)

- Поглощает шаг 1.7. В пайплайн генерации QA добавить парафраз-шаг: вопрос
  переформулируется LLM так, чтобы не использовать лексику answer_sentence
  (проверка: доля пересечения контентных токенов вопроса и ответа ниже
  порога, иначе повторный парафраз). Исходный вопрос сохраняется в поле
  `question_original` — датасет остаётся совместимым.
- Собрать `wiki_100_qa_hard` из тех же 100 документов (переиспользуются
  эмбеддинги предложений из кэша; заново эмбеддятся только вопросы).
- Приёмка (из 1.7): на hard-датасете разброс HR между стратегиями заметно
  шире CI; seed-retrieval больше не находит ответ по лексике (доля вопросов,
  где ответное предложение в топ-1 первого прохода, ощутимо падает против
  оригинала — зафиксировать до/после).
- **Готово, когда:** артефакт датасета собран, базовая линия всех стратегий
  на нём прогнана (оба режима), числа в этом документе.

> Реализация готова (2026-07-04): `python -m stand harden-dataset
> --source-name wiki_100_qa --name wiki_100_qa_hard --qa-provider openai
> --qa-model gpt-5.4-nano`. Парафраз — `src/question_paraphraser.py`:
> метрика `content_token_overlap` (доля контентных токенов вопроса,
> встречающихся в answer_sentence; стоп-слова исключены), приёмка
> кандидата при overlap ≤ 0.35 (`--max-overlap`), до 3 попыток с явным
> списком запрещённых слов, накопляемым из «протекших» токенов. Ответ и
> answer_sentence не меняются — ground truth сохраняется; оригинал вопроса
> пишется в `question_original`, факт-метрика в `paraphrase_overlap`
> (поля переживают roundtrip артефакта). Тесты:
> `tests/test_question_paraphraser.py`.
>
> **Выполнено (2026-07-04).** Генерация: 285/300 вопросов принято
> (overlap ≤ 0.35), средний overlap 0.773 → 0.181 — лексическое
> зеркалирование практически устранено. Baseline на `wiki_100_qa_hard`
> (все стратегии, default, tiktoken):
>
> | Стратегия | per_doc: tokens/HR@5/MRR | shared: tokens/HR@5/MRR |
> |---|---|---|
> | Naive Chunking | 1088 / 0.953 / 0.770 | 1092 / 0.880 / 0.701 |
> | Fixed Window | 1054 / 0.930 / 0.803 | 1011 / 0.900 / 0.762 |
> | Token Text | 1215 / 0.937 / 0.751 | 1214 / 0.850 / 0.669 |
> | Semantic Splitter | 1035 / 0.920 / 0.717 | 970 / 0.827 / 0.655 |
> | Dynamic Semantic | 1681 / 0.973 / 0.903 | 1536 / 0.943 / 0.842 |
>
> Ablation'ы dynamic на hard (per_document): seeds-only — 147 ток.,
> HR 0.793, MRR 0.486; окно ±1 — 572 ток., HR 0.960, MRR 0.826.
>
> Файлы: `benchmark_wiki_100_qa_hard_per_document_20260704_221258.json`
> (полный baseline), `..._shared_20260704_222345.json` (полный baseline),
> `..._per_document_20260704_222539.json` (seeds-only),
> `..._per_document_20260704_222647.json` (±1).
>
> Выводы по приёмке:
>
> 1. **Датасет стал чувствительнее.** В shared разброс HR между
>    стратегиями вырос с 7.0 до 11.6 п.п. (~4× CI ±0.03); все стратегии
>    просели на 2–9 п.п. В per_document разброс умеренный (5.3 п.п.,
>    ~1.8× CI) — hard-эффект сильнее проявляется при конкуренции
>    документов.
> 2. **Ценность экспансии на hard выросла:** seeds-only теряет уже
>    18 п.п. HR против default (было 12); отрыв dynamic от fixed_window
>    в shared вырос с 2.0 до 4.3 п.п.
> 3. **Точка ±1 усилилась:** HR 0.960 против 0.930 у fixed_window при
>    54% его токенов (572 vs 1054) — на hard dynamic ±1 уже не «не хуже»,
>    а значимо лучше по HR при вдвое меньшем бюджете. Формальную
>    значимость проверить в 1.2 (bootstrap CI).
> 4. Замер «ответ в топ-1 первого прохода» до/после не инструментирован
>    (нужна пер-вопросная телеметрия seed'ов) — закрыть при 1.2/1.4.

### Шаг П.3. Dual-space эмбеддинги: phantom для запроса, clean для соседей — M — ✅ выполнено (2026-07-04)

- Гипотеза: с запросом должен сравниваться фантомный эмбеддинг предложения
  (recall: предложение-в-контексте), а сосед с соседом — чистые эмбеддинги
  (когерентность без искусственного текстового перекрытия). Сейчас одна
  фантомная матрица обслуживает обе роли (индекс, `neighbor_sims`,
  `sentence_sims`).
- Реализация: два набора эмбеддингов на предложение. `strategies.py` — индекс
  и `sentence_sims` остаются фантомными, `neighbor_sims` строится из чистых;
  ядро уже принимает массивы раздельно, меняется только сборка. `stand/tune.py`
  — HPO-корпус строит adjacency в том же чистом пространстве (иначе суррогат
  снова разойдётся с боевым путём); режим включить в ключ corpus-cache.
  Стоимость ×2 эмбеддингов при индексации гасится кэшем 2.2.
- Обязательный ретюн: в чистом пространстве adjacency-косинусы ниже и шире,
  текущие пороги не переносятся. Протокол этапа 3: Optuna на train-документах,
  held-out val, сравнение с базой.
- Оценивать на `wiki_100_qa_hard` (и позже qasper) — на оригинальном датасете
  эффект замаскирован потолком (см. П.1).
- **Критерий принятия:** на hard-датасете HR_val не хуже phantom-only базы
  (в пределах CI) при меньших токенах, либо HR выше при тех же токенах;
  вторично — рост interpretability порога (диапазон adjacency-скоров шире).

> **Реализация (2026-07-04):** `DynamicSemanticExpander` принимает опциональную
> матрицу `adjacency_embeddings` (не задана → поведение бит-в-бит прежнее,
> закрыто parity-тестами); `adjacency_space` ("phantom"|"clean") — override-ключ
> dynamic_semantic (A/B через `--dynamic-overrides` без правок кода) и параметр
> `stand tune --adjacency-space` (входит в ключ corpus-cache суффиксом
> `__adj_clean`, пишется в tuned-params и манифест, так что `--params tuned`
> воспроизводит пространство автоматически). Дефолт кода — phantom.
>
> **Распределения adjacency-косинусов** (wiki_100_qa_hard + bge-small,
> n=16948): phantom p5/p50/p95 = 0.747/0.907/0.976; clean = 0.432/0.603/0.789.
> Чистое пространство ниже и в ~1.6 раза шире — механизм систематического
> завышения подтверждён количественно. `stand tune` теперь печатает эти
> перцентили перед трайалами (проверка границ поиска порогов).
>
> **Попутная находка — вырожденность HPO-контура на hard (класс 2.4/3.1):**
> (а) стоковые оконные диапазоны (min_window 1–5, max_expand 3–10) не выражают
> конфиги ниже ~1300 токенов: все 200 трайалов первого phantom-ретюна пробили
> стену 1200, HPO деградировал в «least over budget»; (б) adaptive threshold с
> floor 0.7·base делает пороги в фантомном пространстве нефункциональными —
> даже при τ=0.99 floor 0.693 < p5=0.747, и суррогат даёт 1293–1327 токенов на
> всём диапазоне порогов (расширение параметрам не подчиняется). Введены
> конфиги поиска: `configs/hpo_phantom_adjacency.yaml` и
> `configs/hpo_clean_adjacency.yaml` — оконные диапазоны одинаковые
> (min_window 0–5, max_expand 1–10, зона ±1 из П.1 достижима), пороговые — по
> распределению каждого пространства (clean: 0.30–0.90). Находка (а) → 2.4,
> (б) → аргумент за замену механизма порогов в 3.1/3.2.
>
> **Результаты** (оба тюна: 300 трайалов, train 70%/val 30%, split_seed 42,
> стена 1200, objective с токен-тайбрейкером):
>
> | конфиг | tokens | HR@5 | MRR |
> |---|---:|---:|---:|
> | default phantom, per_doc (база П.2) | 1681 | 0.973 | 0.903 |
> | default clean, per_doc | 1381 | 0.973 | 0.878 |
> | tuned phantom, val-суррогат | 946 | 0.9444 | 0.832 |
> | tuned clean, val-суррогат | 863 | 0.9444 | 0.821 |
> | tuned phantom, live per_doc | 981 | 0.9667 | 0.856 |
> | tuned clean, live per_doc | 931 | 0.9667 | 0.852 |
> | tuned phantom, live shared | 848 | 0.9300 | 0.808 |
> | tuned clean, live shared | 833 | 0.9300 | 0.808 |
>
> Tuned-параметры: phantom — τ=0.653/skip=0.618 (ниже p1 своего пространства,
> т.е. нерабочие), min_window=5, max_expand=2 — токены контролируются
> геометрией окна; clean — min_window=0, τ=0.764 (≈p92), skip=0.717 —
> расширение чисто семантическое, порог стал рабочей ручкой.
>
> Файлы: `..._per_document_20260704_225758.json` (default clean A/B),
> `..._per_document_20260704_230909.json` + `..._shared_20260704_231349.json`
> (tuned phantom), `..._per_document_20260704_231808.json` +
> `..._shared_20260704_232244.json` (tuned clean); манифесты —
> `artifacts/tuned/wiki_100_qa_hard__bge-small/manifest_{phantom,clean}.json`.
>
> **Выводы:**
>
> 1. **Критерий принятия выполнен.** HR идентичен во всех парных сравнениях
>    (val 0.9444 = 0.9444; live 0.9667 = 0.9667; shared 0.9300 = 0.9300) при
>    меньших токенах у clean: −18% на дефолтах, −9% на val, −5%/−2% live.
>    MRR: −2.5 п.п. на дефолтах, паритет в tuned-точке (Δ ≤ 0.3 п.п., глубоко
>    внутри шума).
> 2. **Вторичный критерий (interpretability) выполнен наглядно:** clean-тюн
>    перешёл от геометрического контроля токенов к пороговому (min_window=0,
>    τ≈p92 распределения), phantom-тюн пороги игнорирует (ниже p1).
> 3. **Сверка суррогат ↔ боевой путь на dual-space** (полный корпус, clean
>    best): HR 0.9667 = 0.9667, MRR 0.8507 vs 0.8524, токены 927.5 vs 930.6
>    (<0.4%) — суррогат валиден и для нового пути.
> 4. **Тезис проекта на hard с tuned-параметрами продемонстрирован:** против
>    fixed_window per_doc — HR 0.9667 vs 0.930 при 931 vs 1054 токенов; shared
>    — 0.9300 vs 0.900 при 833 vs 1011. Лучшее качество за меньшие токены в
>    обоих режимах (формальную значимость закрыть в 1.2).
> 5. В shared-режиме обе tuned-конфигурации сходятся к идентичным
>    HR/MRR/NDCG — конкуренция документов нивелирует различия сигнала
>    расширения; расхождение только в токенах.

### Гейт блока П

Выводы по обеим гипотезам зафиксированы в плане; `wiki_100_qa_hard` — новый
основной датасет для этапа 3; решение о судьбе фантомного adjacency-сигнала
принято (заменён/оставлен как опция).

> **Гейт закрыт (2026-07-04).** Обе гипотезы блока разрешены: потолок
> опровергнут в строгой форме, но глубокая экспансия на оригинальном датасете
> сжата (П.1); hard-датасет собран и чувствительнее (П.2); dual-space принят
> по обоим критериям (П.3). **Решение по adjacency-сигналу:** clean —
> рабочее пространство для tuned-прогонов (текущий tuned-артефакт
> `wiki_100_qa_hard__bge-small` — clean; пространство едет в params и
> воспроизводится `--params tuned` автоматически). Дефолт кода остаётся
> phantom, чтобы не менять задним числом опубликованные default-базовые
> линии; окончательная судьба фантомного adjacency решится вместе с
> механизмом порогов в 3.1/3.2 (там же — вывод о floor adaptive threshold).
> Эксперименты этапа 3 вести на clean adjacency.

---

## Этап 1. Честная измерительная база

**Цель этапа:** отчёты отвечают на вопрос проекта («то же качество за меньше
токенов») со статистической строгостью.

### Шаг 1.1. Точный счёт токенов — S — ✅ выполнено (2026-07-04)

- tiktoken вместо chars/4 в `stand/runner.py` и в кэшированном пути (единая
  функция).

> Итог: единая функция — `src/tokens.py::count_tokens` (tiktoken,
> `cl100k_base`, `disallowed_special=()` для служебных строк в корпусе;
> fallback chars/4 с RuntimeWarning для окружений без tiktoken). Используется
> в `stand/runner.py`, в HPO-пути (`expansion_core.evaluate_retrieval` —
> теперь предложения кластера склеиваются пробелами, как в боевом
> `dynamic_retriever`, а не суммируются посимвольно) и в legacy-скриптах
> `run_comparison.py`/`benchmark_failures.py`. Тесты: `tests/test_tokens.py`.
>
> Смена шкалы: на wiki_10 tiktoken ≈ 0.89 от chars/4, т.е. токен-числа
> упали ~11%. Следствия: (1) старые result JSON несравнимы с новыми по
> токенам; (2) жёсткая стена HPO 1200 токенов фактически ослабла на ~11% —
> при следующем `stand tune` это новая шкала, существующие tuned-артефакты
> оценены в старой. Новая базовая линия (хвост гейта этапа 0, полный
> `wiki_100_qa`) должна прогоняться уже на tiktoken-счёте.

### Шаг 1.2. Значимость различий — M — ✅ выполнено (2026-07-05)

- В `summarize_results`: paired bootstrap по вопросам для ΔHR/ΔMRR между
  dynamic_semantic и каждым бейзлайном; 95% CI в таблице. Данные уже лежат в
  `aggregate` результата — только пост-обработка.
- Контекст: при n=300 и HR≈0.95 стандартная ошибка ≈ 0.0126, 95% CI ≈ ±0.025 —
  текущие разницы HR в README внутри шума.
- **Готово, когда:** таблица печатает CI и помечает незначимые различия.

> Итог: чистый модуль `src/significance.py` — парный бутстреп по вопросам
> (10 000 ресемплов, перцентильный 95% CI, seed 42). Парность даёт сам стенд:
> все стратегии оцениваются на одних вопросах в одном порядке; стратегия,
> упавшая на части документов, непарна и пропускается с предупреждением.
> `stand run` считает сравнения против dynamic_semantic автоматически: блок
> `comparisons` в result JSON + таблица с пометкой `ns`. Для сохранённых
> файлов — `stand significance RESULT [--baseline-result BASE]`: tuned-прогон
> динамики сшивается с бейзлайн-прогоном по порядку вопросов (guard по
> dataset/index_mode/metric_k).
>
> Замер на tuned dual-space результатах (см. П.3/README) против бейзлайнов,
> Δ = dynamic − baseline, `*` = значимо (CI не содержит 0):
>
> | vs Fixed Window (сильнейший) | ΔHR@5 [95% CI] | ΔMRR [95% CI] |
> |---|---|---|
> | hard, per_document | **+0.037 [+0.007, +0.067] \*** | **+0.050 [+0.014, +0.085] \*** |
> | hard, shared | +0.030 [−0.003, +0.063] ns | **+0.046 [+0.007, +0.085] \*** |
> | wiki, per_document | +0.017 [+0.000, +0.037] ns | **+0.053 [+0.021, +0.084] \*** |
> | wiki, shared | +0.017 [−0.010, +0.043] ns | **+0.040 [+0.005, +0.074] \*** |
>
> Против остальных бейзлайнов: ΔMRR значим во всех 16 комбинациях
> (бейзлайн × датасет × режим), от +0.040 до +0.153; ΔHR значим в 8 из 12
> (ns: Naive в обоих per_document, Token Text hard per_document на границе
> [+0.000, +0.060], Fixed Window — три ячейки выше).
>
> **Выводы.** (1) Тезис проекта подтверждён со статистической строгостью в
> слабой форме везде: HR нигде значимо не хуже ни одного бейзлайна (все 16
> дельт положительные), MRR везде значимо лучше, токены везде меньше.
> (2) В сильной форме («HR значимо выше при меньших токенах против
> сильнейшего бейзлайна») — показан на hard per_document. (3) Приёмка П.2
> дополнительно подтверждена: разрыв с Fixed Window по HR значим только на
> hard-датасете — оригинал сжат потолком.

### Шаг 1.3. Кривые HR vs токены — M

- Режим стенда: свип бюджета (у dynamic — `target_clusters`/бюджет, у
  бейзлайнов — `top_k`/`chunk_size`) → JSON с точками кривой на стратегию →
  сравнение Парето-фронтов и «HR при выровненном бюджете».
- Становится главным артефактом сравнения вместо однострочной таблицы:
  сравнение стратегий в разных токен-бюджетах конфаундится.

### Шаг 1.4. SNR-метрика — S

- Доля извлечённых токенов в пределах ±k предложений от ответа. Добавить в
  `compute_all_metrics` и таблицу. P@5 сейчас системно наказывает
  dynamic_semantic за слияние кластеров, не измеряя «чистоту» контекста.

### Шаг 1.5. Latency — S

- p50/p95 времени `retrieve()` на вопрос по стратегиям в результат и таблицу.
  Стенд меряет токены, но не runtime-цену экспансии.

### Шаг 1.6. Воспроизводимость — S

- В result JSON: git commit, hash конфига и датасета, seed'ы, версия схемы.

### Шаг 1.7. Усложнённый датасет (чувствительность бенчмарка) — M — ⏫ поглощён шагом П.2

- При генерации QA добавить парафраз-шаг (вопрос переформулируется без лексики
  answer_sentence); собрать `wiki_100_qa_hard`. LLM-вопросы лексически зеркалят
  ответ → HR-потолок ~0.95 сжимает различия стратегий.
- **Готово, когда:** на hard-датасете разброс HR между стратегиями заметно
  шире CI.
- Перенесён в приоритетный блок П (шаг П.2) — делается до остального этапа 1.

### Гейт этапа 1

«Эталонный отчёт» (кривые + CI + SNR + latency) по новой базовой линии на трёх
датасетах: wiki, wiki_hard, qasper.

---

## Этап 2. Ускорение стенда

**Цель этапа:** итерация «гипотеза → результат» занимает минуты, а не часы.

### Шаг 2.1. Батчинг эмбеддингов — S/M — ✅ выполнено (2026-07-04)

- `get_text_embedding_batch` в `src/strategies.py` и `stand/tune.py`;
  `embed_batch_size` в конфиг эмбеддера. Сейчас эмбеддинги считаются по одному
  в Python-цикле.
- Замерить до/после на индексации wiki_100 (ожидание: ×10+ на локальной
  модели).

> Итог: предложения стратегии — один батч-вызов на весь корпус стратегии;
> в tune предложения батчатся по-статейно, вопросы — одним общим батчем.
> `embed_batch_size` (по умолчанию 64, env `EMBEDDING_BATCH_SIZE`) прокинут
> во все провайдеры, включая внутренний батчинг `VectorStoreIndex` у
> бейзлайнов (было 10).
>
> Замер (sanity wiki_10, 3 документа, dynamic+fixed, per_document, CPU):
> 160.6s → 103.3s (~×1.6). Профиль: bge-small на CPU compute-bound —
> батч 641 фантомных текстов 31.0s vs 52.2s поштучно (~×1.7); CUDA на
> машине недоступна (CPU-сборка torch, 6 потоков задействованы). Ожидание
> «×10+» реализуется на GPU или с API-провайдерами (в ~batch_size раз
> меньше HTTP-запросов). Главный рычаг wall-clock на CPU — шаг 2.2:
> эмбеддить один раз на (датасет × эмбеддер × режим) и переиспользовать.

### Шаг 2.2. Разделяемый кэш эмбеддингов — M — ✅ выполнено (2026-07-04)

- Артефакт «(датасет × эмбеддер × режим контекста) → эмбеддинги предложений +
  матрицы», используемый и tune, и run. Кэш эмбеддингов вопросов — один на
  прогон (сейчас до 6 вычислений на вопрос: по разу на стратегию плюс повторно
  внутри экспандера).
- Ключевой эффект: параметры экспансии не трогают индекс, поэтому свипы
  параметров dynamic_semantic не пере-эмбеддят вообще.

> Итог: реализовано контентно-адресуемо, а не по ключу (датасет × эмбеддер):
> `src/embedding_cache.py` — `CachingEmbedding` оборачивает любую
> LlamaIndex-модель и мемоизирует векторы на диске по sha256 самого текста
> (float32, один pkl на (провайдер, модель), атомарная запись). Раз ключ —
> текст, переиспользование автоматическое: tune и run делят векторы, потому
> что фантомные тексты у них идентичны (`build_embedding_texts`), а вопрос,
> заданный N стратегиям, эмбеддится один раз. Query-эмбеддинги — отдельный
> неймспейс от text (у bge один и тот же текст в двух режимах даёт разные
> векторы: cosine 0.965, проверено). Проводка — `stand/embeddings.py::
> prepare_embed_model` (общий для runner и tune); отключение —
> `EMBEDDING_CACHE=0`.
>
> Замер (sanity wiki_10, 3 документа, dynamic+fixed, per_document, CPU):
> холодный кэш 94.1s → тёплый 5.5s (~×17), 0 промахов, метрики бит-в-бит
> совпадают между прогонами. Свипы параметров dynamic_semantic теперь не
> пере-эмбеддят вообще; инвалидация по содержимому получена бесплатно
> (изменился текст → новый ключ), шаг 2.3 остаётся актуальным только для
> corpus-cache tune (матрицы сходства).

### Шаг 2.3. Инвалидация кэша по содержимому — S

- Ключ корпус-кэша сейчас — только имена датасета и эмбеддера. Добавить hash
  data.jsonl + идентификатор модели в манифест кэша; несовпадение → пересборка
  с предупреждением.

### Шаг 2.4. HPO-контур — M

- Вынести `create_objective` из `run_optuna.py` в `src/` (устранить импорт
  библиотечного кода из CLI-скрипта в `stand/tune.py`).
- `stand tune`: storage на ключ (dataset, embedding), фиксированный seed
  сэмплера (сейчас study без storage и seed — теряется и невоспроизводим).
- Multi-objective режим (NSGA-II: max HR, min tokens) → Парето-фронт как
  артефакт (стыкуется с 1.3).
- MedianPruner с промежуточными report каждые ~100 вопросов; вынести создание
  экспандера из цикла по вопросам.

### Шаг 2.5. Консолидация скриптов — S/M

- Корневые `run_comparison.py`, `evaluate_params.py`, `benchmark_failures.py`,
  `analyze_failures.py`, `prepare_failures_corpus.py`, `summarize_results.py` →
  подкоманды `stand` или `scripts/` с пометкой legacy (по
  `docs/target_state.md`).

---

## Этап 3. Эксперименты по качеству

Общий протокол для каждой гипотезы: реализация в едином ядре (из 0.2) → Optuna
на train-документах → held-out метрики → сравнение с базовой линией по
протоколу этапа 1 (CI, кривые). Датасеты: wiki_hard + qasper; контроль
переносимости — второй эмбеддер (например, e5 или API-модель).

### Шаг 3.1. H1: Rank/quantile-пороги — M

- Порог экспансии как квантиль внутридокументного распределения
  adjacency-скоров (параметр p вместо абсолютного τ). Обоснование: косинусные
  скоры эмбеддеров анизотропны (Ethayarajh, 2019), абсолютный порог не
  переносится между моделями и пространствами (чистое vs фантомное).
- **Критерий принятия:** HR_val не хуже базы (в пределах CI) на основном
  эмбеддере И деградация при смене эмбеддера без ретюна значимо меньше, чем у
  абсолютного порога.

### Шаг 3.2. H2: Depth-based границы (TextTiling) — M

- Предвычисляемые границы документов по depth score (Hearst, 1997):
  d(i) = (peak_left − sim_i) + (peak_right − sim_i), граница при
  d > μ_d + c·σ_d; экспансия — до ближайшей границы. Убирает `threshold`,
  `gradient_cliff_factor`, decay-параметры в пользу одного интерпретируемого c.
  Границы предвычислимы (не зависят от запроса) — заодно быстрее.
- **Критерий принятия:** HR_val ≥ база − 1 п.п. при токенах ≤ база − 15%,
  значимо по bootstrap. При успехе — удалить adaptive threshold/gradient stop
  из ядра.

### Шаг 3.3. H3: Centroid-критерий — M

- Включение кандидата по сходству с центроидом кластера (average-link) вместо
  цепочки current↔next (single-link с классической патологией chaining/дрейфа).
  Текущий adaptive threshold сравнивает скор из query-пространства с
  симилярити из adjacency-пространства — распределения разные, сравнение
  некалибровано.
- Комбинируется с 3.2 (граница = depth ∨ centroid-отсечка) — прогнать оба
  варианта.
- **Критерий принятия:** снижение токенов при том же HR на длинных однородных
  документах (qasper), значимо.

### Шаг 3.4. H4: Глобальный токен-бюджет + MMR-жадность — L

- Экспансия как жадная максимизация
  u(s) = α·rel(q, s) + (1−α)·coh(s, cluster) − redundancy при бюджете B токенов
  (MMR: Carbonell & Goldstein, 1998). Убирает `max_expand`/`min_window`/
  `target_clusters`; бюджет перераспределяется от слабых seed'ов к сильным.
- Самое глубокое изменение — делать после результатов 3.1–3.3, на лучшей из
  подтверждённых конфигураций.
- **Критерий принятия:** Парето-фронт доминирует фронт базовой линии (по 1.3)
  хотя бы на одном датасете и не хуже на остальных.

### Шаг 3.5. H5: Late chunking (параллельный трек) — L

- Отдельный «режим эмбеддинга» стенда: один проход длинно-контекстной модели
  (jina-v3 / nomic-embed), пулинг токенных эмбеддингов по предложениям; для
  bge — оконный вариант с перекрытием (контекст 512 токенов). Сравнить: без
  контекста vs phantom vs late chunking при фиксированной стратегии.
  Обоснование: Günther et al. (Jina, 2024); фантомные эмбеддинги стоят
  (2w+1)× по токенам эмбеддера и имеют жёсткое окно.
- **Критерий принятия:** late chunking ≥ phantom по HR при меньшей стоимости
  индексации (замер из 2.1).

### Шаг 3.6. Финал: обновление README и отчёта — S

- Итоговые таблицы с CI, Парето-кривые, честные выводы (включая то, что не
  подтвердилось), обновление CONCEPT_DESIGN под фактическую архитектуру.

---

## Сводка зависимостей

- 0.1 → 0.2 → (0.3, весь этап 3)
- 0.5 → гейт этапа 0 (меняет базовую линию — делать до перепрогона)
- Блок П — сразу после гейта этапа 0 и шага 1.1: П.1 → П.2 → П.3;
  П.2 поглощает 1.7; П.3 требует кэша 2.2 (готов) и стыкуется с 3.1
- Этап 1 (остаток: 1.2–1.6) — после блока П или параллельно с П.2/П.3
- 2.1–2.3 независимы, желательно до этапа 3; 2.4 — до 3.1
- 3.1 → 3.2 → 3.3 → 3.4 (последовательно, каждый на подтверждённой базе);
  3.5 — параллельно; все эксперименты этапа 3 — на `wiki_100_qa_hard`
  (+ qasper), не на оригинальном wiki-датасете (потолок, см. П.1)

Минимальный критический путь до первого научного результата:
**0.1 → 0.2 → 0.3 → 0.6 → гейт → 1.1 → П.1 → П.2 → П.3 → 1.2 → 3.1**.

---

## Пересмотр политики штрафов HPO (2026-07-04)

Старый objective внутри лимита токенов был `HR·100 + MRR·0.01`, а
`token_bonus_weight = 0` — то есть ниже стены 1200 токены бесплатны. Тезис
проекта («то же качество за меньше токенов») в objective не кодировался:
конфиг с 1199 токенами побеждал конфиг с 400 токенами за один случайно
пойманный вопрос (ΔHR 0.0033 — глубоко внутри CI ±0.025 при n=300). Optuna
систематически выбирала самые раздутые из равных по качеству конфигураций.

Сделано (`src/hpo_config.py`):

- `ObjectivePolicy.token_tiebreak_fraction` (по умолчанию 0.5): вес
  токен-бонуса выводится динамически как
  `fraction · hr_weight / num_valid_questions`, т.е. максимум бонуса — это
  доля стоимости одного пойманного вопроса. Так экономия токенов **никогда**
  не перевешивает даже один вопрос HR (протокол «max HR при бюджете» не
  меняется), но среди равных по HR всегда выигрывает дешёвый. Порядок
  приоритетов: HR ≫ токены ≫ MRR. `fraction=0` → старое статическое
  поведение.
- Скоринг вынесен в чистую `compute_objective_score` (общая для objective и
  оценки train/val); свойство тай-брейкера закреплено тестами
  `tests/test_hpo_objective.py`.
- Жёсткая стена не тронута: это осмысленный протокол «выровненного бюджета»
  (~1160 токенов у fixed_window). Честный обмен «HR ↔ токены» без весов —
  мульти-объектив NSGA-II (2.4) + Парето-кривые (1.3).
- `stand tune` получил `--hpo-config` (search space + objective overrides);
  политика скоринга целиком пишется в tuned-манифест (`tuning.objective`).

## Приложение: прочие замечания из аудита

- **Query- vs text-режим эмбеддинга вопросов (найдено при 2.2, класс 0.3).**
  bge эмбеддит один и тот же текст по-разному в query- и text-режиме
  (cosine 0.965 на bge-small, проверено эмпирически). Сейчас: боевой первый
  retrieval-pass ранжирует seed'ы в query-пространстве; HPO-суррогат
  (`stand/tune.py` эмбеддит вопросы `get_text_embedding_batch`) — в
  text-пространстве; query-aware проверка экспандера
  (`dynamic_retriever._query_sims`) — тоже в text-пространстве (консистентно
  с HPO, но не с первым pass'ом). То есть seed-ранжирование суррогата и
  боевого пути живут в разных пространствах. Закрыть при 2.4 или перед
  выводами по HPO: либо tune переводить на query-эмбеддинги вопросов, либо
  боевой путь — на text-режим для первого pass'а (решение зафиксировать).
- Seed validation (отключён): фильтрация высоких выбросов Modified Z-score
  противоречит цели retrieval — топ-результат и должен быть выбросом. Если
  возвращаться, то как к калибровке уверенности (score₁/score₂), а не к
  отсечению одиночек. Аннотация `_validate_seed -> bool` не соответствует
  фактическому возврату кортежа.
- Cross-encoder rerank кластеров — стандартный буст MRR/NDCG, но ортогонален
  гипотезе о чанкинге; имеет смысл как отдельная ось стенда.
- Downstream-метрика (answer F1 c LLM) из CONCEPT_DESIGN не реализована — хотя
  бы на подвыборке 50 вопросов закрыла бы вопрос «помогает ли компактный
  контекст генерации».
